// Build-time constants injected by vite.config.ts `define` — the short git
// commit of the running build and when it was built. "dev" under `vite dev`.
declare const __APP_VERSION__: string;
declare const __BUILD_AT__: string;
